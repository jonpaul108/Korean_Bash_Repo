import {
  connect
} from 'react-redux';
import QuizPiece from '../components/quizPiece.jsx';


const mapStateToProps = state => ({

});

const mapDispatchToProps = (dispatch) => ({

});

const VisuaQuizPiece = connect(mapStateToProps, mapDispatchToProps)(QuizPiece);

export default VisualHome;