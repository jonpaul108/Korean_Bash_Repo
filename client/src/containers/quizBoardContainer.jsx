import {
  connect
} from 'react-redux';
import QuizBoard from '../components/quizBoard.jsx';


const mapStateToProps = state => ({

});

const mapDispatchToProps = (dispatch) => ({

});

const VisualQuizBoard = connect(mapStateToProps, mapDispatchToProps)(QuizBoard);

export default VisualQuizBoard;